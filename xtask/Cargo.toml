[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
publish = false

[dependencies]
lk-jwt-service-e2e-tests = { path = "../e2e-tests" }
reqwest = { version = "0.13", default-features = false }
tokio = { version = "1", features = ["rt", "time"] }
